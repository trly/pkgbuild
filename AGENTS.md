# Repository Instructions

- This repository contains independent Arch Linux packages; make changes inside the affected top-level package directory only.
- Each package is defined by `PKGBUILD`; keep its tracked `.SRCINFO` synchronized with `makepkg --printsrcinfo > .SRCINFO` from that package directory.
- An Arch package is a zstd-compressed `tar` archive generated by `makepkg`: it contains installable binaries plus `.PKGINFO` (pacman metadata), `.BUILDINFO` (reproducible-build information), `.MTREE` (file hashes and timestamps), and optional `.INSTALL` and `.Changelog` files when the `PKGBUILD` specifies them.
- After testing package functionality, check both metadata and the built archive with `namcap PKGBUILD` and `namcap pkgname.pkg.tar.zst`; namcap checks common `PKGBUILD` and hierarchy errors, shared-library dependencies in ELF files, and missing or redundant dependencies.
- Use `pkgctl build` from the package directory to test a clean chroot build and inspect warnings; for dependencies from the AUR, provide built packages with `pkgctl build -I path/to/somepkg.tar.gz -I ...`.
- CI runs the clean build as `sudo --user=builder --set-home pkgctl build` from the package directory; it requires an Arch `base-devel` environment and a non-root build user.
- CI checks only packages whose directory contains a changed file and a `PKGBUILD`; changes outside package directories do not trigger package checks.
- Package build outputs and downloaded sources (`pkg/`, `src/`, `*.pkg.tar.*`, `*.src.tar.*`, and package-specific source archives) are ignored; do not treat them as source changes.
- Preserve the package-local `REUSE.toml` SPDX annotations when adding or changing package metadata files.

## Additional References
- https://wiki.archlinux.org/title/Creating_packages
- https://man.archlinux.org/man/PKGBUILD.5
